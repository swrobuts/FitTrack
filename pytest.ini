[pytest]
testpaths = backend/tests
# Starlette 1.6 warnt intern vor einer veralteten anyio-Schreibweise. Das betrifft
# nicht unseren Code, deshalb blenden wir genau diese Warnung aus.
filterwarnings =
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
